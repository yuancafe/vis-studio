Use Pencil to generate a product UI screen system aligned with approved brand rules.

Objective:
{goal}

Source of truth:
{required_documents}

Constraints:
- Keep hierarchy, tone, and component logic consistent with approved UI guidance.
- Prefer system-level structure over isolated screens.
- Keep screens suitable for downstream design iteration.

Required outputs:
- UI screen system brief
- screen generation prompt
